version: '3.7'

services:

  httpd:
    image: nginx:latest
    expose:
      - "${HTTPD_LISTEN_PORT}"
    ports:
      - "${HTTPD_LISTEN_PORT}:${HTTPD_LISTEN_PORT}"
    volumes:
      - ./vendor/prooxxy/gleipnir/components/httpd/nginx.conf:/etc/nginx/nginx.conf
      - ./vendor/prooxxy/gleipnir/components/httpd/upstream.conf:/etc/nginx/upstream.conf
      - ./vendor/prooxxy/gleipnir/components/httpd/m2.conf:/etc/nginx/m2.conf
      - ./vendor/prooxxy/gleipnir/components/httpd/sites:/etc/nginx/sites-available
      - ./vendor/prooxxy/gleipnir/components/ssl/certificates:/etc/nginx/certificates
      - .:/var/www/html
    depends_on:
      - php
      - database

  php:
    build:
      context: ./vendor/prooxxy/gleipnir/components/php/${PHP_VERSION}
      dockerfile: Dockerfile
    ports:
      - "9000"
      - "9001"
    user: "${USER}"
    env_file:
      - ./vendor/prooxxy/gleipnir/components/.env.php
    volumes:
      - ./vendor/prooxxy/gleipnir/components/php/${PHP_VERSION}/conf/php.ini:/usr/local/etc/php/php.ini
      - ./vendor/prooxxy/gleipnir/components/php/${PHP_VERSION}/conf/php-fpm.conf:/usr/local/etc/php-fpm.conf
      - ./vendor/prooxxy/gleipnir/components/php/${PHP_VERSION}/conf/www.conf:/usr/local/etc/www.conf
      - .:/var/www/html
    extra_hosts:
      - host.docker.internal:$HOST_IP

  database:
    image: mysql:${MYSQL_VERSION}
    ports:
      - "${DB_LISTEN_PORT}:${DB_LISTEN_PORT}"
    env_file:
      - ./vendor/prooxxy/gleipnir/components/.env.mysql
    expose:
      - "${DB_LISTEN_PORT}"
    volumes:
      - db-persistance:/var/lib/mysql
      - ./vendor/prooxxy/gleipnir/components/mysql/conf.d:/etc/mysql/conf.d

  redis:
    image: redis:latest
    volumes:
      - redis-persistance:/data

  rabbitmq:
    image: rabbitmq:${RABBITMQ_VERSION}-management-alpine
    hostname: ${PROJECT_NAME}-rabbitmq
    env_file:
      - ./vendor/prooxxy/gleipnir/components/.env.rabbitmq
    volumes:
        - rabbitmq-persistance:/var/lib/rabbitmq

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:6.0.0
    environment:
      - "http.host=0.0.0.0"
      - "transport.host=0.0.0.0"
      - "ES_JAVA_OPTS=-Xms1g -Xmx1g"
      - "discovery.type=single-node"
    volumes:
      - es-persistance:/usr/share/elasticsearch/data
    ports:
      - 9200:9200
      - 9300:9300

  mailhog:
    image: mailhog/mailhog
    ports: 
      - 1025:1025
      - 8025:8025

volumes:
    db-persistance:
      name: ${PROJECT_NAME}-db-data
    es-persistance:
      name: ${PROJECT_NAME}-es-data
    rabbitmq-persistance:
      name: ${PROJECT_NAME}-rabbit-data
    redis-persistance:
      name: ${PROJECT_NAME}-redis-data

networks:
  default:
      ipam:
          config:
              - subnet: 10.123.0.0/16      